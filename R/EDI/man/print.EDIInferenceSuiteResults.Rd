% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_suite.R
\name{print.EDIInferenceSuiteResults}
\alias{print.EDIInferenceSuiteResults}
\title{Prints the results table from an \code{\link[EDI:InferenceSuite]{InferenceSuite}}
\code{run_all_inference()} call -- the same table \code{screen = TRUE} prints during
the call itself, so a user who assigned the return value and later types its name
(or calls \code{print()} on it) sees a readable table rather than a raw nested list
dump.}
\usage{
\method{print}{EDIInferenceSuiteResults}(x, ...)
}
\arguments{
\item{x}{An \code{EDIInferenceSuiteResults} object, as returned by
\code{InferenceSuite$run_all_inference()}.}

\item{...}{Ignored; present for S3 consistency with the generic.}
}
\value{
\code{x}, invisibly.
}
\description{
Prints the results table from an \code{\link[EDI:InferenceSuite]{InferenceSuite}}
\code{run_all_inference()} call -- the same table \code{screen = TRUE} prints during
the call itself, so a user who assigned the return value and later types its name
(or calls \code{print()} on it) sees a readable table rather than a raw nested list
dump.
}
