% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_abstract.R
\name{Design}
\alias{Design}
\title{An Abstract Experimental Design}
\description{
Internal method.
An abstract R6 Class encapsulating the data and functionality for an experimental design.
This class takes care of data storage and response handling.
}
\details{
Throughout the package, treatment assignment vectors \eqn{w} use the
\eqn{\{0, 1\}} encoding: \eqn{1} indicates a treated subject and \eqn{0}
a control subject.  All public methods that return or accept \eqn{w}
(e.g. \code{get_w()}, \code{draw_ws_according_to_design()}) use this
convention. A handful of variance estimators (e.g. \code{InferenceIncidCMH},
\code{InferenceIncidExtendedRobins}) recode to a signed \eqn{\{-1,+1\}}
contrast internally where their formulas require it; that recoding is
local to those classes and does not affect this public convention.
}
\examples{
\dontrun{
# Design is abstract and cannot be instantiated directly; construct a
# concrete subclass instead, e.g.:
seq_des = DesignSeqOneByOneBernoulli$new(n = 6, response_type = 'continuous')
seq_des$add_one_subject_to_experiment_and_assign(data.frame(x1 = rnorm(1)))
}
}
\keyword{internal}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{num_cores}}{Current number of cores in the global budget.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-Design-is_blocking_design}{\code{Design$is_blocking_design()}}
    \item \href{#method-Design-is_matching_design}{\code{Design$is_matching_design()}}
    \item \href{#method-Design-is_a_kk_matching_capable}{\code{Design$is_a_kk_matching_capable()}}
    \item \href{#method-Design-is_a_cluster_capable}{\code{Design$is_a_cluster_capable()}}
    \item \href{#method-Design-is_a_bernoulli_capable}{\code{Design$is_a_bernoulli_capable()}}
    \item \href{#method-Design-initialize}{\code{Design$new()}}
    \item \href{#method-Design-add_one_subject_response}{\code{Design$add_one_subject_response()}}
    \item \href{#method-Design-add_all_subject_responses}{\code{Design$add_all_subject_responses()}}
    \item \href{#method-Design-overwrite_all_subject_assignments}{\code{Design$overwrite_all_subject_assignments()}}
    \item \href{#method-Design-is_fixed_sample_size}{\code{Design$is_fixed_sample_size()}}
    \item \href{#method-Design-assert_all_subjects_arrived}{\code{Design$assert_all_subjects_arrived()}}
    \item \href{#method-Design-assert_all_responses_recorded}{\code{Design$assert_all_responses_recorded()}}
    \item \href{#method-Design-check_experiment_completed}{\code{Design$check_experiment_completed()}}
    \item \href{#method-Design-assert_even_allocation}{\code{Design$assert_even_allocation()}}
    \item \href{#method-Design-assert_fixed_sample}{\code{Design$assert_fixed_sample()}}
    \item \href{#method-Design-any_censoring}{\code{Design$any_censoring()}}
    \item \href{#method-Design-has_general_censoring}{\code{Design$has_general_censoring()}}
    \item \href{#method-Design-get_t}{\code{Design$get_t()}}
    \item \href{#method-Design-get_X_raw}{\code{Design$get_X_raw()}}
    \item \href{#method-Design-get_X_imp}{\code{Design$get_X_imp()}}
    \item \href{#method-Design-get_X}{\code{Design$get_X()}}
    \item \href{#method-Design-get_y}{\code{Design$get_y()}}
    \item \href{#method-Design-get_y_original}{\code{Design$get_y_original()}}
    \item \href{#method-Design-get_w}{\code{Design$get_w()}}
    \item \href{#method-Design-draw_ws_according_to_design}{\code{Design$draw_ws_according_to_design()}}
    \item \href{#method-Design-capabilities}{\code{Design$capabilities()}}
    \item \href{#method-Design-supports}{\code{Design$supports()}}
    \item \href{#method-Design-applicable_inference_class_names}{\code{Design$applicable_inference_class_names()}}
    \item \href{#method-Design-unavailable_inference_classes_due_to_missing_packages}{\code{Design$unavailable_inference_classes_due_to_missing_packages()}}
    \item \href{#method-Design-randomization_family}{\code{Design$randomization_family()}}
    \item \href{#method-Design-supports_resampling}{\code{Design$supports_resampling()}}
    \item \href{#method-Design-supports_randomization_draw}{\code{Design$supports_randomization_draw()}}
    \item \href{#method-Design-supports_resampling_replay}{\code{Design$supports_resampling_replay()}}
    \item \href{#method-Design-prepare_for_resampling_replay}{\code{Design$prepare_for_resampling_replay()}}
    \item \href{#method-Design-warm_all_subject_data_cache}{\code{Design$warm_all_subject_data_cache()}}
    \item \href{#method-Design-get_n}{\code{Design$get_n()}}
    \item \href{#method-Design-get_y_L}{\code{Design$get_y_L()}}
    \item \href{#method-Design-get_y_R}{\code{Design$get_y_R()}}
    \item \href{#method-Design-get_effective_time}{\code{Design$get_effective_time()}}
    \item \href{#method-Design-get_effective_dead}{\code{Design$get_effective_dead()}}
    \item \href{#method-Design-get_prob_T}{\code{Design$get_prob_T()}}
    \item \href{#method-Design-get_response_type}{\code{Design$get_response_type()}}
    \item \href{#method-Design-get_response_type_original}{\code{Design$get_response_type_original()}}
    \item \href{#method-Design-get_ordinal_levels}{\code{Design$get_ordinal_levels()}}
    \item \href{#method-Design-get_original_ordinal_levels}{\code{Design$get_original_ordinal_levels()}}
    \item \href{#method-Design-get_missingness_method}{\code{Design$get_missingness_method()}}
    \item \href{#method-Design-transform_y}{\code{Design$transform_y()}}
    \item \href{#method-Design-get_design_formula}{\code{Design$get_design_formula()}}
    \item \href{#method-Design-duplicate}{\code{Design$duplicate()}}
    \item \href{#method-Design-clone}{\code{Design$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-is_blocking_design"></a>}}
\if{latex}{\out{\hypertarget{method-Design-is_blocking_design}{}}}
\subsection{\code{Design$is_blocking_design()}}{
  Check whether this design currently has blocking structure.

The base implementation returns \code{FALSE}. Designs that compose
\code{BlockingStructure} override this method with the structural check.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$is_blocking_design()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{FALSE} for designs without \code{BlockingStructure}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-is_matching_design"></a>}}
\if{latex}{\out{\hypertarget{method-Design-is_matching_design}{}}}
\subsection{\code{Design$is_matching_design()}}{
  Check whether this design currently has matching structure.

The base implementation returns \code{FALSE}. Designs that compose
\code{MatchingStructure} override this method with the structural check.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$is_matching_design()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{FALSE} for designs without \code{MatchingStructure}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-is_a_kk_matching_capable"></a>}}
\if{latex}{\out{\hypertarget{method-Design-is_a_kk_matching_capable}{}}}
\subsection{\code{Design$is_a_kk_matching_capable()}}{
  Characterization: is this a KK matching-on-the-fly-capable
  design (sequential KK or its fixed binary-match equivalent)? Default
  \code{FALSE}; overridden to \code{TRUE} on
  \code{DesignSeqOneByOneKK14} and \code{DesignFixedBinaryMatch}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$is_a_kk_matching_capable()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-is_a_cluster_capable"></a>}}
\if{latex}{\out{\hypertarget{method-Design-is_a_cluster_capable}{}}}
\subsection{\code{Design$is_a_cluster_capable()}}{
  Characterization: is this a cluster-structured design?
  Default \code{FALSE}; overridden to \code{TRUE} on
  \code{DesignFixedCluster} and \code{DesignFixedBlockedCluster}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$is_a_cluster_capable()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-is_a_bernoulli_capable"></a>}}
\if{latex}{\out{\hypertarget{method-Design-is_a_bernoulli_capable}{}}}
\subsection{\code{Design$is_a_bernoulli_capable()}}{
  Characterization: is this a Bernoulli-randomized design?
  Default \code{FALSE}; overridden to \code{TRUE} on
  \code{DesignSeqOneByOneBernoulli} and \code{DesignFixedBernoulli}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$is_a_bernoulli_capable()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-Design-initialize}{}}}
\subsection{\code{Design$new()}}{
  Initialize an experimental design
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$new(
  response_type,
  prob_T = 0.5,
  include_is_missing_as_a_new_feature = FALSE,
  n = NULL,
  verbose = FALSE,
  missingness_method = "impute",
  design_formula = ~.,
  ordinal_levels = NULL,
  seed = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{response_type}}{"continuous", "incidence", "proportion", "count", "survival", or
"ordinal".}
      \item{\code{prob_T}}{Probability of treatment assignment.}
      \item{\code{include_is_missing_as_a_new_feature}}{Flag for missingness indicators.}
      \item{\code{n}}{The sample size (if fixed).}
      \item{\code{verbose}}{Flag for verbosity.}
      \item{\code{missingness_method}}{How to handle missing values in covariates when building the
model matrix for inference. One of:
\describe{
  \item{\code{"impute"} (default)}{Missing values are filled in using random-forest
    imputation (\code{missRanger}, falling back to \code{missForest} on failure).
    The response vector is included as an auxiliary predictor when available.
    This preserves all covariates and all subjects but introduces imputed values
    that influence inference.}
  \item{\code{"drop_column"}}{Any covariate column that contains at least one
    missing value is dropped entirely from the model matrix before inference.
    No values are invented; the remaining complete columns are used as-is.
    This is conservative but transparent.}
  \item{\code{"error"}}{An error is thrown as soon as any missing value is
    detected in the covariate matrix. Use this when you want to guarantee that
    inference runs on exactly the data you supplied, with no silent modification.}
}}
      \item{\code{design_formula}}{A formula object used to create the design matrix from
covariates. Default is \code{~ .}.}
      \item{\code{ordinal_levels}}{If the response type is "ordinal", the labels for the levels.}
      \item{\code{seed}}{Integer seed for reproducibility.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new `Design` object
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-add_one_subject_response"></a>}}
\if{latex}{\out{\hypertarget{method-Design-add_one_subject_response}{}}}
\subsection{\code{Design$add_one_subject_response()}}{
  For CARA designs, add a single subject response.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$add_one_subject_response(t, y = NULL, y_L = NULL, y_R = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{t}}{The subject index.}
      \item{\code{y}}{The exact response value. Supply this XOR both \code{y_L}
and \code{y_R} -- never together, never just one of the two.}
      \item{\code{y_L}}{For a censored survival response, the lower bound of the
event-time interval. Right-censored: the last known event-free
time (pair with \code{y_R = Inf}). Left-censored: \code{0},
which must be stated explicitly rather than defaulted.
Interval-censored: the interval's lower bound. Storage accepts
any well-formed left-/interval-censored value; whether a given
\code{Inference} class can actually consume it depends on that
class (most survival \code{Inference} classes still only accept
exact/right-censored data and will reject construction with a
clear error otherwise -- see individual class docs).}
      \item{\code{y_R}}{For a censored survival response, the upper bound of the
event-time interval. Right-censored: \code{Inf}. Left-/
interval-censored: the confirmed-by time / interval upper bound.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-add_all_subject_responses"></a>}}
\if{latex}{\out{\hypertarget{method-Design-add_all_subject_responses}{}}}
\subsection{\code{Design$add_all_subject_responses()}}{
  For non-CARA designs, add all subject responses.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$add_all_subject_responses(ys = NULL, y_Ls = NULL, y_Rs = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{ys}}{The exact responses as a numeric vector, \code{NA} for any
subject whose response is censored (supply \code{y_Ls}/\code{y_Rs}
for those instead).}
      \item{\code{y_Ls}}{The censored-response lower bounds, \code{NA} for any
subject with an exact response in \code{ys}. Right-censored:
the last known event-free time (pair with \code{y_Rs = Inf}).
Left-censored: \code{0}, stated explicitly. Interval-censored:
the interval's lower bound. Storage accepts any well-formed
left-/interval-censored value; whether a given \code{Inference}
class can actually consume it depends on that class (most
survival \code{Inference} classes still only accept exact/
right-censored data and will reject construction with a clear
error otherwise -- see individual class docs).}
      \item{\code{y_Rs}}{The censored-response upper bounds, \code{NA} for any
subject with an exact response in \code{ys}. Right-censored:
\code{Inf}. Left-/interval-censored: the confirmed-by time /
interval upper bound.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-overwrite_all_subject_assignments"></a>}}
\if{latex}{\out{\hypertarget{method-Design-overwrite_all_subject_assignments}{}}}
\subsection{\code{Design$overwrite_all_subject_assignments()}}{
  For analysis on already-completed experimental data
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$overwrite_all_subject_assignments(w)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{w}}{A \{0,1\} vector of subject assignments (1 = treated, 0 = control).}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-is_fixed_sample_size"></a>}}
\if{latex}{\out{\hypertarget{method-Design-is_fixed_sample_size}{}}}
\subsection{\code{Design$is_fixed_sample_size()}}{
  Check if this design was initialized with a fixed sample size n
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$is_fixed_sample_size()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    TRUE if fixed.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-assert_all_subjects_arrived"></a>}}
\if{latex}{\out{\hypertarget{method-Design-assert_all_subjects_arrived}{}}}
\subsection{\code{Design$assert_all_subjects_arrived()}}{
  Asserts if all subjects arrived.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$assert_all_subjects_arrived()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-assert_all_responses_recorded"></a>}}
\if{latex}{\out{\hypertarget{method-Design-assert_all_responses_recorded}{}}}
\subsection{\code{Design$assert_all_responses_recorded()}}{
  Asserts if all responses are recorded.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$assert_all_responses_recorded()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-check_experiment_completed"></a>}}
\if{latex}{\out{\hypertarget{method-Design-check_experiment_completed}{}}}
\subsection{\code{Design$check_experiment_completed()}}{
  Checks if the experiment is completed.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$check_experiment_completed()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} if experiment is complete, \code{FALSE} otherwise.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-assert_even_allocation"></a>}}
\if{latex}{\out{\hypertarget{method-Design-assert_even_allocation}{}}}
\subsection{\code{Design$assert_even_allocation()}}{
  Checks if the experiment has a 50-50 allocation.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$assert_even_allocation()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-assert_fixed_sample"></a>}}
\if{latex}{\out{\hypertarget{method-Design-assert_fixed_sample}{}}}
\subsection{\code{Design$assert_fixed_sample()}}{
  Checks if the experiment has a fixed sample size.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$assert_fixed_sample()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-any_censoring"></a>}}
\if{latex}{\out{\hypertarget{method-Design-any_censoring}{}}}
\subsection{\code{Design$any_censoring()}}{
  Checks if the experiment has any censored responses
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$any_censoring()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} if any censored.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-has_general_censoring"></a>}}
\if{latex}{\out{\hypertarget{method-Design-has_general_censoring}{}}}
\subsection{\code{Design$has_general_censoring()}}{
  Checks if the experiment has any left- or
  interval-censored survival responses -- i.e. any subject whose
  \code{y_R} is finite (right-censored subjects have
  \code{y_R = Inf}, which is excluded). Most survival
  \code{Inference} classes cannot yet consume this shape of data
  (see \code{get_effective_time()}/\code{get_effective_dead()});
  this is the check \code{Inference$initialize()} uses to reject
  construction cleanly for those classes.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$has_general_censoring()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} if any subject is left- or interval-censored.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_t"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_t}{}}}
\subsection{\code{Design$get_t()}}{
  Get t
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_t()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The current number of subjects.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_X_raw"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_X_raw}{}}}
\subsection{\code{Design$get_X_raw()}}{
  Get raw X information
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_X_raw()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A data frame of subject data.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_X_imp"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_X_imp}{}}}
\subsection{\code{Design$get_X_imp()}}{
  Get imputed X information
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_X_imp()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    Same as \code{Xraw} except with imputations.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_X"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_X}{}}}
\subsection{\code{Design$get_X()}}{
  Get X matrix
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_X()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A numeric matrix of subject data.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_y"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_y}{}}}
\subsection{\code{Design$get_y()}}{
  Get y
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_y()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A numeric vector of subject responses.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_y_original"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_y_original}{}}}
\subsection{\code{Design$get_y_original()}}{
  Get y_original
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_y_original()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A numeric vector of the original subject responses.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_w"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_w}{}}}
\subsection{\code{Design$get_w()}}{
  Get w
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_w()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A \{0,1\} vector of subject assignments (1 = treated, 0 = control).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-draw_ws_according_to_design"></a>}}
\if{latex}{\out{\hypertarget{method-Design-draw_ws_according_to_design}{}}}
\subsection{\code{Design$draw_ws_according_to_design()}}{
  Draw treatment assignment vectors according to the design.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$draw_ws_according_to_design(r = 1L)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{r}}{Number of vectors to draw. Default is 1.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A matrix of size n x r with \{0,1\} entries (1 = treated, 0 = control).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-capabilities"></a>}}
\if{latex}{\out{\hypertarget{method-Design-capabilities}{}}}
\subsection{\code{Design$capabilities()}}{
  Returns the capabilities this design \strong{instance} exposes
  (see \code{fix_design_hierarchy.md}, "Capability Model").

  Deliberately instance-level, not a class-registry read (fix_design_hierarchy.md,
  TODO-28): \code{is_blocking_design()}/\code{is_matching_design()} depend on
  real construction-time state (e.g. \code{private$m}/\code{private$blocking_capable}),
  not just which components a class composes -- \code{DesignFixediBCRD}
  constructed with an unknown \code{n}, for instance, composes
  \code{BlockingStructure} but is \emph{not} blocking-capable for that particular
  instance. A class-registry-only answer (this function briefly unioned in
  \code{get_effective_design_capabilities()}, a purely class-level, component-
  composition-based check) would silently report "blocking" for every instance
  of such a class regardless of its actual construction state -- confirmed as a
  real, reproducible false positive during this TODO's implementation, not a
  hypothetical. \code{get_effective_design_capabilities()}/
  \code{design_class_registry.R}'s \code{direct_components} still exist and are
  correct -- they're the right tool for a \strong{generator}-only query with no
  instance in hand (see \code{design_class_generator_supports_batch_w_pregeneration()}),
  just not for this instance-level method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$capabilities()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A character vector of capability names.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-supports"></a>}}
\if{latex}{\out{\hypertarget{method-Design-supports}{}}}
\subsection{\code{Design$supports()}}{
  Returns whether this design object supports a capability.
  See \code{capabilities()}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$supports(capability)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{capability}}{A capability name, e.g. \code{"blocking"},
\code{"matching"}, or \code{"batch_w_pregeneration"}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} if the capability is present, \code{FALSE} otherwise.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-applicable_inference_class_names"></a>}}
\if{latex}{\out{\hypertarget{method-Design-applicable_inference_class_names}{}}}
\subsection{\code{Design$applicable_inference_class_names()}}{
  Returns the sorted character vector of concrete, exported
  \code{Inference} class names legal for this design object under
  default constructor arguments, derived purely from this design's own
  normalized metadata (response type, KK-matching capability, blocking,
  and both censoring axes) filtered through the registry's
  compatibility predicates -- the same normalization and predicate
  logic \code{\link[EDI:InferenceSuite]{InferenceSuite}} uses for
  discovery (see \code{normalize_inference_design_metadata()} and
  \code{is_inference_class_compatible_with_design_metadata()} in
  \code{inference_suite.R}). No candidate class is constructed to
  determine applicability, so this has no side effects and cannot be
  influenced by a constructor failure or a missing optional package
  (see \code{unavailable_inference_classes_due_to_missing_packages()}
  for that case, reported separately). A class whose censoring
  tolerance depends on non-default constructor arguments (e.g.
  \code{InferenceSurvivalCoxPHRegr} only tolerates general censoring
  with \code{testing_type = "wald"}) is listed here when its
  \strong{default} configuration is compatible; a construction-time
  error for an incompatible non-default argument combination remains
  the documented behavior of that class's \code{initialize()}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$applicable_inference_class_names()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A sorted character vector of applicable \code{Inference} class
  names.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-unavailable_inference_classes_due_to_missing_packages"></a>}}
\if{latex}{\out{\hypertarget{method-Design-unavailable_inference_classes_due_to_missing_packages}{}}}
\subsection{\code{Design$unavailable_inference_classes_due_to_missing_packages()}}{
  Companion to \code{applicable_inference_class_names()}:
  returns the subset of otherwise design-compatible \code{Inference}
  classes that are excluded solely because a registered
  \code{required_packages} entry is not installed, as a named list
  (class name -> character vector of missing package names) -- kept
  separate from plain design incompatibility so callers can tell "not
  applicable to this design" apart from "applicable, but an optional
  dependency isn't installed."
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$unavailable_inference_classes_due_to_missing_packages()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A named list, class name -> missing package names; empty list
  if none.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-randomization_family"></a>}}
\if{latex}{\out{\hypertarget{method-Design-randomization_family}{}}}
\subsection{\code{Design$randomization_family()}}{
  Returns this design object's registry-backed randomization
  family (see \code{fix_design_hierarchy.md}, "Class Metadata"), e.g.
  \code{"kk14"}, \code{"bernoulli"}, \code{"rerandomization"}. Replaces
  class-identity (\code{inherits()}/\code{is()}) dispatch at call sites that
  need to distinguish design variants (see "Class-Identity Dispatch
  Replacement"). Returns \code{NA_character_} if the class is not registered
  or is one of the unsplit/timing-root abstract bases.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$randomization_family()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A single character string (or \code{NA_character_}).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-supports_resampling"></a>}}
\if{latex}{\out{\hypertarget{method-Design-supports_resampling}{}}}
\subsection{\code{Design$supports_resampling()}}{
  Check if the design supports resampling at all -- \code{FALSE}
  only for the abstract timing-family bases themselves (\code{DesignFixed},
  \code{DesignSeqOneByOne}, and their custom-extension abstract bases)
  instantiated directly; \code{TRUE} for every concrete subclass,
  \strong{including} \code{ObservationalDesign}. This is the general check
  for resampling methods that never need the design's own randomization
  mechanism -- plain nonparametric bootstrap, Bayesian bootstrap,
  m-out-of-n bootstrap, PRW subsampling -- which only resample already-observed
  units/rows and their fixed, observed assignment, so they remain valid and
  available even for a design with no randomization mechanism at all (see
  \code{ObservationalDesign}'s class documentation: "resampling subjects with
  their observed, fixed assignment does not require a known randomization
  probability"). Contrast with \code{supports_randomization_draw()}/
  \code{supports_resampling_replay()} below, which gate the narrower set of
  methods that actually do need to invoke the design's mechanism (a plain
  randomization test/CI, or a bootstrap randomization test that re-randomizes
  resampled data) and are therefore \code{FALSE} for \code{ObservationalDesign}
  specifically -- see \code{fix_design_hierarchy.md}, "Observational Design
  Migration" for the live bug that split fixes.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$supports_resampling()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    TRUE if supported.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-supports_randomization_draw"></a>}}
\if{latex}{\out{\hypertarget{method-Design-supports_randomization_draw}{}}}
\subsection{\code{Design$supports_randomization_draw()}}{
  Check if this design can draw a fresh treatment assignment
  from its own randomization mechanism -- the eligibility condition for
  permutation-style randomization tests/CIs (\code{compute_rand_two_sided_pval()}
  and friends), which redraw \eqn{w} directly. \code{FALSE} for the abstract
  timing-family bases themselves (same as \code{supports_resampling()}) and,
  unlike \code{supports_resampling()}, also \code{FALSE} for
  \code{ObservationalDesign} (no draw mechanism at all -- \eqn{w} is supplied
  by the user, so there is nothing to redraw); \code{TRUE} for every other
  concrete subclass. See \code{supports_resampling()}'s documentation for why
  this is a narrower, separate capability rather than reusing that one, and
  "Observational Design Migration" for the live bug this fixes
  (\code{ObservationalDesign} previously answered the old, unsplit
  \code{supports_resampling()} \code{TRUE}, silently passing the
  randomization-test eligibility assert before failing later and deeper,
  inside \code{draw_ws_raw()}'s throwing stub).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$supports_randomization_draw()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    TRUE if a fresh randomization draw is supported.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-supports_resampling_replay"></a>}}
\if{latex}{\out{\hypertarget{method-Design-supports_resampling_replay}{}}}
\subsection{\code{Design$supports_resampling_replay()}}{
  Check if this design's mechanism can be faithfully replayed
  against resampled data -- the eligibility condition specifically for the
  bootstrap \emph{randomization test} (BRT), which resamples units and then
  re-randomizes each resample using the design's own mechanism (see
  \code{inference_all_abstract_rand_bootstrap.R}'s repeated
  \code{draw_ws_according_to_design()} calls). \strong{Not} the eligibility
  condition for plain nonparametric/Bayesian/m-out-of-n/PRW-subsampling
  bootstrap -- those never redraw \eqn{w} at all (they resample already-observed
  units and their fixed, observed assignment) and are gated by the broader
  \code{supports_resampling()} instead, which stays \code{TRUE} for
  \code{ObservationalDesign}. \code{FALSE} for the same abstract timing-family
  bases as \code{supports_randomization_draw()} and for
  \code{ObservationalDesign} (no randomization mechanism to replay); \code{TRUE}
  for every other concrete subclass. See \code{supports_randomization_draw()}'s
  documentation for why this is a separate capability rather than the same flag
  reused.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$supports_resampling_replay()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    TRUE if bootstrap-randomization-test-style replay is supported.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-prepare_for_resampling_replay"></a>}}
\if{latex}{\out{\hypertarget{method-Design-prepare_for_resampling_replay}{}}}
\subsection{\code{Design$prepare_for_resampling_replay()}}{
  Hook invoked by the bootstrap-randomization-test machinery
  on a design object whose assignment mechanism is about to be replayed
  against resampled data (once per replicate draw site, ahead of
  \code{draw_ws_according_to_design(1L)}). The base implementation is a
  no-op; designs whose replay is a full re-optimization
  (\code{DesignFixedOptimal}) override it to switch to their
  per-replicate solver profile (\code{solver_args$brt_*}). Idempotent.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$prepare_for_resampling_replay()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{invisible(NULL)}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-warm_all_subject_data_cache"></a>}}
\if{latex}{\out{\hypertarget{method-Design-warm_all_subject_data_cache}{}}}
\subsection{\code{Design$warm_all_subject_data_cache()}}{
  Warm the per-subject assignment-data cache, when this
  design uses covariates. This is an internal optimization hook for
  randomization inference; it keeps cache mutation inside the Design
  object instead of exposing its private environment to callers.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$warm_all_subject_data_cache()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{TRUE} invisibly when a cache warm-up was attempted, or
  \code{FALSE} invisibly when the design does not use covariates.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_n"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_n}{}}}
\subsection{\code{Design$get_n()}}{
  Get n, the sample size
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_n()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The number of subjects.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_y_L"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_y_L}{}}}
\subsection{\code{Design$get_y_L()}}{
  Get y_L
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_y_L()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A numeric vector of censored-response lower bounds
  (\code{NA} for exact-response subjects).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_y_R"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_y_R}{}}}
\subsection{\code{Design$get_y_R()}}{
  Get y_R
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_y_R()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A numeric vector of censored-response upper bounds
  (\code{NA} for exact-response subjects).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_effective_time"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_effective_time}{}}}
\subsection{\code{Design$get_effective_time()}}{
  Get the effective response time per subject: the exact
  value \code{y} where recorded, or the lower bound \code{y_L}
  for a censored subject. This reconstructs "the one informative
  number" every response type other than left-/interval-censored
  survival data has always had, for code that needs a single
  numeric value per subject rather than the \code{y}/\code{y_L}/
  \code{y_R} triple directly.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_effective_time()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A numeric vector, one value per subject.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_effective_dead"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_effective_dead}{}}}
\subsection{\code{Design$get_effective_dead()}}{
  Get the effective event indicator per subject: \code{1}
  for an exact response, \code{0} for a censored one. This
  reconstructs today's \code{dead} semantics for right-censored
  survival data (and is trivially all-\code{1} for every other
  response type, which never has censoring). It is only valid
  for exact/right-censored data -- a left- or interval-censored
  subject also returns \code{0} here, which is not meaningful
  right-censoring status, so callers must confirm (e.g. via
  \code{any_censoring()} plus their own censoring-shape checks)
  that no such rows are present before relying on this value.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_effective_dead()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    An integer vector, one value per subject.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_prob_T"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_prob_T}{}}}
\subsection{\code{Design$get_prob_T()}}{
  Get probability of treatment
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_prob_T()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The specified probability.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_response_type"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_response_type}{}}}
\subsection{\code{Design$get_response_type()}}{
  Get response type
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_response_type()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The specified response type.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_response_type_original"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_response_type_original}{}}}
\subsection{\code{Design$get_response_type_original()}}{
  Get the original response type
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_response_type_original()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The original specified response type.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_ordinal_levels"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_ordinal_levels}{}}}
\subsection{\code{Design$get_ordinal_levels()}}{
  Get ordinal levels
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_ordinal_levels()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The levels of the ordinal response.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_original_ordinal_levels"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_original_ordinal_levels}{}}}
\subsection{\code{Design$get_original_ordinal_levels()}}{
  Get original ordinal levels
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_original_ordinal_levels()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The labels for the levels of the original ordinal response.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_missingness_method"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_missingness_method}{}}}
\subsection{\code{Design$get_missingness_method()}}{
  Get the missingness method
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_missingness_method()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The missingness handling method: \code{"impute"}, \code{"drop_column"},
  or \code{"error"}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-transform_y"></a>}}
\if{latex}{\out{\hypertarget{method-Design-transform_y}{}}}
\subsection{\code{Design$transform_y()}}{
  Transform the response vector y
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$transform_y(
  transform_fun,
  transformed_response_type,
  ordinal_levels = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{transform_fun}}{A function that takes y_original and returns a new y.}
      \item{\code{transformed_response_type}}{The response type of the transformed y.}
      \item{\code{ordinal_levels}}{If the transformed response type is "ordinal", the labels for the levels.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-get_design_formula"></a>}}
\if{latex}{\out{\hypertarget{method-Design-get_design_formula}{}}}
\subsection{\code{Design$get_design_formula()}}{
  Get the model formula
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$get_design_formula()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The model formula.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-duplicate"></a>}}
\if{latex}{\out{\hypertarget{method-Design-duplicate}{}}}
\subsection{\code{Design$duplicate()}}{
  Duplicate this design object
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$duplicate(verbose = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{verbose}}{A flag for verbosity.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new `Design` object with the same data
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Design-clone"></a>}}
\if{latex}{\out{\hypertarget{method-Design-clone}{}}}
\subsection{\code{Design$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{Design$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
